Placez ici le logo de l application avec ce nom :

- `logo.png`

Chemin attendu par l interface :

- `assets/branding/logo.png`

Si le fichier n est pas encore present, l application affichera automatiquement un embleme de secours inspire du logo.
